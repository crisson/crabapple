import React from 'react';

export default React.createClass({

    displayName: 'Spinner',

    render() {
        return (
            <div className="content content-spinner"></div>
        );
    }

});